Firt add the file to your branch, with "git add <file>" command.
"Git commit -m <commit message>" and then "git push".
If you want to merge with development, go with "git checkout <branchname>" to the main branch and with "git merge <branchname>".

Rebase is when we integrating changes from one branch onto another, with all the commits/history.